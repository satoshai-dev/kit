---
'@satoshai/kit': minor
---

Enrich `WalletInfo` with `name`, `icon`, and `webUrl` fields sourced from `@stacks/connect`'s provider metadata. This enables displaying wallet icons, names, and install links in custom UIs without hardcoding wallet data. Resolves #4.
